import React from "react";

// Make the message better and make it look better
const Nothing = () => {
    return (
        <div>
            <h1>Nothing Found</h1>
        </div>
    );
};

export default Nothing;
